#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int n, i, fact=1;
    scanf("%d", &n);
    
    for(i=n; i>=1; i--){
        fact *= i;
    }
    printf("%d", fact);
    return 0;
}
