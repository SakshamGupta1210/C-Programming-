#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int N;
    scanf("%d", &N);  // Read the input number

    for (int i = 1; i <= N; i++) {
        if (N % i == 0) {
            printf("%d ", i);  // Print factor
        }
    }
    return 0;
}
